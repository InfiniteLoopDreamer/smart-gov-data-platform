# 截图清单

截图保存到 `docs/screenshots/`，与 README 文件名一致。

| 文件 | 画面 |
| --- | --- |
| `01-login.png` | 登录页，可见 admin / user 提示 |
| `02-home.png` | 政府首页看板（KPI + 趋势 + 区域 + 热门事项） |
| `03-cases.png` | 诉求工单处置（筛选 + 表格） |
| `04-trend.png` | 趋势分析 / 预测页 |

## 截图步骤

1. 后端：`uvicorn backend.main:app --host 127.0.0.1 --port 8001`
2. 前端：`cd frontend && npm run dev` → http://127.0.0.1:5500
3. `admin / admin123` 登录后按上表截图
4. 浏览器建议宽度 ≥ 1400px
